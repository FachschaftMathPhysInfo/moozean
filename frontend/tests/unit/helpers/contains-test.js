
import { contains } from 'ember-ozean/helpers/contains';
import { module, test } from 'qunit';

module('Unit | Helper | contains', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = contains([42]);
    assert.ok(result);
  });
});


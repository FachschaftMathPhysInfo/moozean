
import { isActive } from 'ember-ozean/helpers/is-active';
import { module, test } from 'qunit';

module('Unit | Helper | is active');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isActive(['moor','moor']);
  assert.ok(result);
});

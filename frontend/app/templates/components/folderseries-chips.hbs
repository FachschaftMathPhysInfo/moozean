{{#paper-chips removeItem=(action "removeObj") addItem=(action "addObj") placeholder="Ordnerreihen" content=content options=options search=(action "search") readOnly=disabled searchField="name" requireMatch=true as |item|}}

  <strong>{{item.name}}</strong>
{{/paper-chips}}

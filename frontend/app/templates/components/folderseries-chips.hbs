{{#paper-chips removeItem=(action "removeFolderseries") addItem=(action "addFolderseries") placeholder="Ordnerreihen" content=content options=options searchField="name" requireMatch=true as |item|}}

  <strong>{{item.name}}</strong>
{{/paper-chips}}

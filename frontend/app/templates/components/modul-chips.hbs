{{#paper-chips removeItem=(action "removeModul") addItem=(action "addModul") placeholder="Module" content=content options=options search=(action "searchModuls") searchField="name" requireMatch=true as |item|}}
  <strong>{{item.name}}</strong>
  {{#if item.abbreviation}}
    <em>{{item.abbreviation}}</em>
  {{/if}}
{{/paper-chips}}

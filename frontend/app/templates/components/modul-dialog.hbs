<PaperDialog @class="flex-77" @onClose={{action closeDialog "cancel"}} @origin={{dialogOrigin}} @clickOutsideToClose={{true}}>
  <PaperToolbar>
    <PaperToolbarTools>
      <h2>{{title}}</h2>
      <span class="flex"></span>
      <PaperButton @iconButton={{true}} @onClick={{action closeDialog "cancel"}}><PaperIcon @icon="close" /></PaperButton>
    </PaperToolbarTools>
  </PaperToolbar>
  <PaperForm @onSubmit={{action closeDialog "ok"}} as |form|>
  <PaperDialogContent>
      <div class="flex layout-column">
    <form.input @flex={{30}} @label="Name" @value={{modul.name}} @required={{true}} @onChange={{action (mut modul.name)}} />
    <form.input @flex={{30}} @label="Abkürzung" @value={{modul.abbreviation}} @required={{true}} @onChange={{action (mut modul.abbreviation)}} />
    <form.input @flex={{30}} @label="Link-Modulhandbuch" @value={{modul.linkModulhandbuch}} @required={{true}} @onChange={{action (mut modul.linkModulhandbuch)}} />
    </div>
  </PaperDialogContent>
  <PaperDialogActions @class="layout-row">
    <span class="flex"></span>
    <PaperButton @onClick={{action closeDialog "cancel"}}>Abbrechen</PaperButton>
    <PaperButton @onClick={{action closeDialog "ok" modul}} @disabled={{form.isInvalid}}> OK</PaperButton>
  </PaperDialogActions>
  </PaperForm>
</PaperDialog>
{{yield}}

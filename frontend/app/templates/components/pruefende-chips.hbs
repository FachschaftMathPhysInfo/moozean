{{#paper-chips removeItem=(action "removeExaminator") addItem=(action "addExaminator") placeholder="Prüfende" content=content options=options searchField="surname" search=(action "search") requireMatch=true as |item|}}
  <strong>{{item.surname}}</strong>
  {{#if item.givenname}}
    <em>{{item.givenname}}</em>
  {{/if}}
{{/paper-chips}}
